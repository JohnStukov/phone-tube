package app.phonetube.ui.theme

import androidx.compose.ui.graphics.Color

/** Colores fijos con contraste legible (no dependen del tema Material). */
object ContrastColors {
    val chipSelectedBg = Color(0xFFFFFFFF)
    val chipSelectedText = Color(0xFF0F0F0F)
    val chipUnselectedBg = Color(0xFF272727)
    val chipUnselectedText = Color(0xFFF1F1F1)

    val subscribeBg = Color.Transparent
    val subscribeText = Color(0xFFF1F1F1)
    val subscribedBg = Color(0xFF272727)
    val subscribedText = Color(0xFFF1F1F1)

    val actionBg = Color(0xFF3A3A3A)
    val actionText = Color(0xFFF1F1F1)
    val actionSelectedBg = Color(0xFF4A4A4A)
    val actionSelectedText = Color(0xFFFFFFFF)
}
